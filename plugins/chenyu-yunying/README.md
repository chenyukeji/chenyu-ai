# 晨玙 Amazon 运营插件

`chenyu-yunying` 是面向 Amazon 运营岗位的独立插件。它从产品开发文档、自有产品事实、产品素材和竞品链接出发，完成竞品研究、欧洲站 Listing 和逐图作图要求。当前版本为 `1.0.0`。

最新 Listing 写作与校验标准见 [LISTING_RULES.md](LISTING_RULES.md)。该文件区分 Amazon 平台规则与晨玙内部执行标准，是 Listing Skill、校验脚本和交付模板的统一依据。

插件不登录或修改 Amazon 店铺，不自动发布 Listing，不执行广告投放，也不会把真实开发表、竞品快照、图片或任务输出写入插件源码和分发包。

默认输出位于 `outputs/chenyu-yunying/`，先按 `competitor-research`、`listing`、`image-brief` 或 `combined-delivery` 分大目录，再使用 `YYYY-MM-DD_产品简称` 建立本次任务目录。同一综合任务在各部分复用相同任务名；详细规则见 [输出目录规范](references/output-paths.md)。

## 当前包含什么

| Skill | 适合什么需求 | 主要输入 | 主要交付 | 不负责 |
| --- | --- | --- | --- | --- |
| `chenyu-yunying` | 不确定该用哪个能力，或同时需要 Listing 和作图要求 | 开发表、素材、目标站点、自然语言要求 | 统一产品事实并协调下面三个专业 Skill | 后台自动调度、广告投放、商品发布 |
| `chenyu-jingpin` | 抓取和整理竞品资料 | 开发表链接、Amazon 商品链接或榜单研究要求 | 标题、五点、普通/A+描述、榜单/图片证据及 `competitor-research.json` | 生成自有 Listing、评论痛点分析、复制竞品图片、抓品牌故事 |
| `chenyu-listing` | 生成欧洲站自有产品 Listing | 自有产品事实、目标站点、竞品研究包 | 默认 DE/FR/IT/ES 四站 Item Name、Item Highlights、五点、HTML 详情和 Search Terms；UK 按需生成 | 抓取网页、发布商品 |
| `chenyu-zuotuyaoqiu` | 生成交接美工的逐图方案 | 自有事实、可选 Listing 与竞品视觉证据 | “产品内容＋作图要求”双表 Excel，每张图含目的、构图、文案、嵌入式参考图片和验收条件 | 实际生成或精修图片 |

其中：

- Item Name 使用 2—4 个真实核心关键词，控制在 68—75 字符并以 70—75 为目标；关键差异按购买重要度前置，数量大于 1 时把本地化数量短语放在第一个核心产品词前。
- Item Highlights 必填且不超过 125 字符，用自然句补充标题放不下的关键词、规格、结构和多个场景。前台字段允许自然重复核心词、关键事实、尺寸和场景，不执行机械去重。
- Search Terms 先用主要同款 ASIN 在卖家精灵反查候选，再核对对应 Amazon 站点前 20 个自然结果，最终只保留前台未覆盖的增量词。
- 关键词研究和词频只作为内部写作与校验证据。最终 Listing Excel 默认只有“产品内容”和各站点 Listing，不生成关键词页、关键词栏、词频表或分配检查；用户明确要求关键词分析时才另行交付。
- 各站点 Listing 中，每个变体按“目标站点文案、中文翻译、参考”三列成组；Item Name、Item Highlights、五点、HTML 详情和 Search Terms 都提供完整中文翻译，方便中国卖家核对。Search Terms 参考同时记录主要同款 ASIN、卖家精灵反查来源与 Amazon 相关性复核，不生成审核说明页。
- Listing 的“产品内容”页统一使用“产品名称、产品图片、产品尺寸、产品材料、产品规格、补充信息”六列，并保留开发文档中经核对属于自有产品的图片；能确认变体时按变体展示，不能区分但确认属于本产品时标为共用素材。竞品图和归属不明图片不会混入。
- 五点统一为“相关 Emoji＋【本地语言利益点小标题】＋2—4 句正文”，只计正文时每条必须超过 200 个可见字符；第一句直接给出具体特点和结果。同款产品可采用第一条同款链接中的功能、材质表现、适用范围和使用方式，仍排除品牌、冲突规格、认证与售后承诺。
- 详情使用基础 HTML，依次包含 2—3 句概述、3—5 项详细编号功能、产品参数、可选使用/保养、包装内容和可选注意事项；允许完整展开前台字段中的重点信息。
- 同一任务中的竞品链接只抓取一次，Listing 和作图要求复用同一个研究包。
- 作图单默认采用两张工作表：“产品内容”统一记录产品名称、产品图片和已确认事实；“作图要求”按任务嵌入一张或多张有效竞品参考图并写明各自用途，不用图片超链接，也不重复放产品图；默认规格为 1254×1254 px。
- Excel 统一使用一份“晨玙 Amazon 运营交付大母版”，其中包含产品内容、作图要求和 DE/FR/IT/ES 四站 Listing。任务文件默认删除未要求的模块：只要作图要求时保留产品内容与作图要求，只要四站 Listing 时保留产品内容与四站 Listing；固定流程要求全表时，未要求的工作表可留空但不写占位内容。
- 默认先确定七张图的任务顺序，再为每张图寻找匹配的竞品参考：主图、产品近照、尺寸图、使用流程或细节图、特点/优势或场景图、场景图、四宫格场景图。现有素材不能反向决定图序。
- 图片尽量无文字；必须写字时统一用精简英文。目标站点 Listing 仍使用对应站点语言。
- 用户提出综合 Listing 需求但未另行指定站点时，默认一次生成德国、法国、意大利、西班牙四站版本；各站独立本地化，不把德语稿机械逐句翻译。英国站仅在明确要求时加入。

## 工作流程

```text
开发表、产品素材、目标站点
            ↓
解析工作表、超链接、图片锚点和附件
            ↓
建立自有产品事实与变体记录
            ↓
chenyu-jingpin 抓取一次竞品 Listing 与图片
            ↓
competitor-research.json
       ┌────┴────┐
       ↓         ↓
chenyu-listing  chenyu-zuotuyaoqiu
       ↓         ↓
自有 Listing    逐图作图要求
       └────┬────┘
            ↓
事实、变体、语言和卖点一致性检查
```

## 方法说明

### 1. 证据优先级

插件把“自己的产品事实”和“竞品提供的表达参考”严格分开。发生冲突时按以下优先级处理：

1. 用户在当前任务中的明确说明。
2. 开发文档中的自有产品文字、规格和变体记录。
3. 已确认归属于当前产品/变体的自有图片。
4. 已形成的自有产品事实记录及其来源位置。
5. 竞品 Listing、图片和 A+ 内容，仅用于关键词、表达和构图研究。
6. 通用语言与版式经验，只能帮助组织内容，不能创造产品事实。

竞品页面中的尺寸、材质、配件、功能、认证和品牌不会自动进入自有产品事实。无法追溯到自有证据的内容，要么省略，要么作为具体待确认项保留在受影响字段旁。

### 2. 产品事实如何形成

开发表先被解析为工作表、单元格、超链接、图片锚点和附件清单，再按变体建立事实记录。常用字段包括：

| 事实类别 | 示例字段 | 核对重点 |
| --- | --- | --- |
| 产品身份 | 产品名称、品类、用途 | 不能用竞品名称代替自有产品身份 |
| 规格 | 长、宽、高、直径、重量、容量 | 数值必须绑定真实测量部位和单位 |
| 材质 | 主体材质、表面材质、配件材质 | 材质不能推出未确认的性能 |
| 外观 | 颜色、图案、形状、结构 | 图片归属必须对应具体变体 |
| 包装 | 数量、主体、配件 | 场景道具不计入包装内容 |
| 结构 | 开口、接口、部件、固定方式 | 看不清或未说明的结构不能补画、补写 |
| 场景 | 使用位置、节日、环境 | 只采用产品事实能够支持的场景 |

每项事实保留 `confirmed`、`unconfirmed` 或 `conflict` 状态。只有 `confirmed` 内容才能进入买家可见 Listing 和正式图片卖点。

### 3. 竞品研究如何使用

`chenyu-jingpin` 按站点和 ASIN 去重，读取标题、五点、普通/A+描述、主图、图册和 A+ 图片；任务要求时以各站点相关 Best Sellers 前 50 为候选池。它不主动抓取或新增评论痛点分析。下游只读取这一个研究包，避免 Listing 和作图要求分别抓取后出现证据不一致。

竞品证据分成两类：

| 证据类型 | 可以用于 | 不能用于 |
| --- | --- | --- |
| 文字证据 | 当地产品词、同义词、属性表达、字段分布、独立商品组频次 | 复制品牌、连续文案、竞品规格或功能声明 |
| 视觉证据 | 构图类型、信息层级、产品占比、场景关系、尺寸线表达 | 直接复制竞品图、商标、文案、错误外观或配件 |

抓取失败、字段缺失和访问受限都会保留状态。插件不会用搜索摘要补齐缺失页面，也不会把“抓到竞品”当成任务完成。

## Listing 生成方法

### 1. 从事实到 Listing 字段

每个站点和变体先建立以下映射：

```text
自有产品事实
      ↓
可验证的购买理由
      ↓
目标站点关键词与允许的同义表达
      ↓
标题、五点、详情描述、Search Terms
```

一个购买理由必须能回到开发资料、自有图片，或在同质化同款模式下回到“同款确认＋具体竞品点位”。未确认同款时，竞品高频但自有产品不具备的词不采用；确认同款后可采用反复出现的功能、材质表现、适用范围与使用方式，但不迁移品牌、不同规格、认证和售后承诺。一个事实可以支持多个字段，但不同字段承担不同任务，避免反复改写同一句话。

### 2. 标题依据

每个站点/变体选择 2—4 个不同且已标记为核心词的标题关键词，按自然语言组织：

```text
[已确认品牌] + [数量>1时的本地化数量短语] + 第一核心产品词/关键差异 + 其他核心词 + 高价值属性或场景
```

标题规则：

- 核心产品词优先使用目标站点最准确、最自然的品类表达。
- 售卖数量等于 1 时不写入标题；大于 1 时用目标语言把数量放在第一个核心产品词正前方，例如 `12 Stück Ballons`。数量不属于核心词，采购数量也不能当作售卖数量。
- 能解释价格、购买或适配差异的重要规格放在标题前部；普通尺寸、颜色和内部款号移入 Item Highlights。
- 2—4 个关键词不能只是同一个词的机械重复，也不能用颜色、数量或普通形容词凑数。
- 主要场景为可选项；有高搜索价值且有空间时保留一个，多个场景放入 Item Highlights，不用 `/` 堆叠。
- 子体标题写当前变体差异；单色款可补充一个当前颜色，多色款不枚举颜色；父体标题只在父子体关系和变体主题已核实时提供。
- 只有开发资料或用户明确确认的自有品牌才能加入；否则不添加品牌、品牌占位符、竞品身份、促销承诺或主观“最佳”描述。
- 标点后按目标语言规范留空格；大小写遵循当地语言，不把“五个站点所有单词首字母大写”当成统一规则。
- 非媒体类目 Item Name 不超过 75 字符；内部要求为 68—75 字符，目标 70—75。只能用真实、高相关信息补足长度。

标题中的 2—4 个词必须是关键词研究中标记为核心的短语并进入标题。Item Highlights、五点和详情可以自然重复这些核心词及关键事实，不要求跨字段机械去重。

### 3. Item Highlights 依据

Item Highlights 每个站点和子体必填，使用一句自然当地语言，不超过 125 字符并以 115—125 为编辑目标。优先放标题未容纳的核心/长尾词、重要尺寸、材质结构、安装适配和多个真实场景；可以重复标题中的关键差异，但应补充新的识别信息。

### 4. 五点卖点依据

五点写作前先汇总所有有效竞品链接的五点，按主题合并同义内容并去重，再按独立商品组统计重复度。至少 2 个独立商品组出现或覆盖有效样本半数以上的主题视为高频卖点，必须进入候选五点。默认按照买家决策顺序分工：

1. 产品名称、主要用途和最核心的使用结果。
2. 适用范围、尺寸选择或关键适配条件。
3. 特殊功能/结构及其直接好处。
4. 安装或使用方式、材料性能或另一个高频利益点。
5. 包装内容、颜色、清洁保养或促成购买的补充点。

每条使用“相关 Emoji＋【本地语言利益点小标题】＋2—4 句正文”，只计正文时必须超过 200 个可见字符；第一句必须直接说明具体特点与买家结果，后续句补充适用条件、尺寸边界、使用动作和买家结果。不设置机械最高字符数，但每句话都必须增加具体信息，不能用空泛形容词或同义重复凑长度。

当用户、采购资料或产品证据确认竞品与自有产品为同质化同款时，可直接采用同款竞品反复出现的功能、材质表现、适用范围、安装方式、清洁方式和使用效果，不因开发文档没有逐项写明而删除；在内部事实记录中保留“同款竞品证据＋同款确认”。竞品品牌、ASIN、不同尺寸/数量/配件、认证、质保和售后承诺不迁移，开发资料冲突项也不迁移。没有同款确认时仍按自有事实过滤。

### 5. 详情描述依据

详情描述按以下顺序组织：

1. 产品概括：2—3 句说明产品是什么、主要特点和核心场景。
2. 3—5 项详细编号功能：每项 2—4 句，说明特点、结构/材料、使用方式、买家结果和必要边界。
3. 产品参数：只列已确认的材质、颜色、尺寸及测量部位等。
4. 使用/保养（条件项）：有可靠事实时填写。
5. 包装内容：逐项列明实际数量和物品。
6. 注意事项（条件项）：只写有来源的使用、保养、测量或安全说明。

详情允许完整展开 Item Name、Item Highlights 和五点中的重点信息，不要求机械去重。

默认使用只含 `<p>`、`<br>`、`<b>` 的基础 HTML，概括和特征充分展开。场景中的人物、家具、礼物、植物和装饰品不会被写成包装配件。

### 5. 多站点本地化

综合 Listing 任务默认生成 DE、FR、IT、ES 四站，UK 按需加入。多站点任务先固定英文语义提纲；UK 在目标中时先完成英国站文案，其他站点按标题、每条五点和详情信息块分别本地化，并参考各站点有效榜单样本，不整段逐句翻译：

| 站点 | 核对重点 |
| --- | --- |
| DE | 名词大小写、复合词、语序 |
| FR | 性数、介词、冠词和重音 |
| IT | 冠词、性数和属性搭配 |
| ES | 西班牙当地用语、重音和自然搭配 |
| UK | 英式拼写和当地产品表达 |

各站分别处理产品词、属性词、标题语序、五点小标题、详情栏目名和 Search Terms。事实保持一致，但句式和卖点展开顺序可以不同。

## 关键词方法

### 1. 关键词从哪里来

关键词候选来自三部分：

- 竞品原词：目标站点竞品标题、五点和描述中实际出现的产品词、属性词和场景词。
- 自有事实词：尺寸、材质、颜色、图案、结构和包装等真实属性。
- 本地化语义词：与自有事实一致、符合目标语言习惯的同义表达和长尾组合。

只有已经抓取目标站点竞品并完成独立商品组统计时，才能声称某词在样本中出现了多少次。没有当地竞品样本时，只把词标为相关候选，不声称具有搜索量、排名、热度或转化验证。

### 2. 频次如何计算

竞品频次按“出现该词的独立商品组数量”计算：

- 同一商品标题、五点和描述重复出现多次，只计 1 个商品组。
- 同一父商品的不同抓取入口按研究包中的 `product_group` 去重。
- 同义词先保留原始证据，再在语义组层面汇总。
- 抓取失败或字段缺失不计为未出现，并保留样本分母。

频次帮助判断表达是否常见，但不等于搜索量、广告流量或转化率。

### 6. 关键词如何分配

| 字段 | 作用 | 选择原则 |
| --- | --- | --- |
| 标题关键词 | 建立品类识别和主要购买意图 | 每站 2—4 个不同主词，自然成句 |
| Item Highlights | 补足标题未容纳的信息 | 规格、结构、长尾词和多个场景组成一句自然文案 |
| 核心关键词 | 适合前台自然覆盖的产品、属性和场景词 | 与事实直接相关，通常 10—20 个 |
| 长尾关键词 | 组合尺寸、属性、款式和场景 | 相关且具体，通常 10—15 个 |
| 五点关键词 | 承接标题未覆盖的次要词和购买意图 | 每点尽量自然覆盖一个，不为埋词牺牲可读性 |
| Search Terms | 只补充 Item Name、Item Highlights、五点、详情和其他已填属性尚未覆盖的真实搜索表达 | 先由主要同款 ASIN 在卖家精灵反查，再核对目标 Amazon 站点自然结果；小写、空格分隔、无标点 |

所有关键词都要排除竞品品牌、ASIN、错误规格、无依据功能、主观宣传词和无关流量词。Search Terms 在所有前台字段完成后生成，删除前台已出现词、字段内重复词、停用词和无价值单复数重复，统一小写并以单个空格分隔，控制在 249 UTF-8 字节以内。候选词在目标 Amazon 站点前 20 个自然结果中，同类占比不低于 70% 为高相关，40%—69% 为中等相关，低于 40% 通常排除；美国站数据用于欧洲站时只作语义候选。

以上关键词方法属于内部生成与校验流程。它会影响标题、五点、详情和 Search Terms，但不会默认作为关键词工作表或栏目出现在最终文档中。

### 7. Listing Excel 交付结构

未指定模板时，Listing 工作簿保持精简：

- `产品内容`：固定使用“产品名称、产品图片、产品尺寸、产品材料、产品规格、补充信息”六列，放已确认事实、变体差异以及经核对属于自有产品的图片。
- 每个目标站点一个 Listing 工作表：按变体并列提供子体 Item Name、Item Highlights、五点、HTML 详情和 Search Terms。
- 每个变体均按“目标站点文案、中文翻译、参考”三列成组；所有买家可见字段和 Search Terms 都提供完整中文翻译，参考列记录依据与核验信息。

父体标题仅在父子体关系及变体主题已核实时提供；未核实时不建立空占位。工作簿不默认新增“关键词”“关键词频次”“核心关键词”“长尾关键词”“关键词分配”“标题结构检查”或“审核说明”工作表/栏目。

## 作图要求方法

### 1. 先确定图片任务，再找参考图

作图要求采用“任务优先”逻辑：先确定每张图要解决的买家问题，再从竞品研究包和自有素材中寻找适合该任务的参考。现有素材只能确认产品身份、外观和可执行性，不能反向决定整套图应该做什么。

用户未另行指定时，默认七张图如下：

| 图序 | 默认任务 | 关键要求 |
| --- | --- | --- |
| 1 | 主图 | 纯白背景、完整售卖产品、无文字 |
| 2 | 产品近照图 | 产品细节为主体，加入淡化的真实使用场景或环境局部，默认无文字 |
| 3 | 尺寸图 | 绑定真实测量部位；可加克制边框、角饰或场景边缘，中心测量区保持干净 |
| 4 | 使用流程图或细节图 | 有可验证步骤时做流程；否则做结构/细节图，并用轻量情境辅助理解 |
| 5 | 产品特点或优势图 | 只表现已确认且有区分度的特点，同时加入少量使用场景；没有特点时改为完整场景图 |
| 6 | 场景图 | 一个清晰、自然、产品可见的典型场景 |
| 7 | 四宫格场景图 | 四个有区别的使用场景，产品身份保持一致 |

用户明确指定的数量、顺序和图型优先。某个品类不适用默认槽位时，应保留该槽位的销售目的，替换成最接近且可执行的图型。

除第一张白底主图外，第 2–7 张都可按任务融入场景。第 2–5 张以产品或信息为主、场景为辅助，第 6–7 张以完整场景表达为主；场景不得遮挡产品、尺寸箭头或关键细节，也不能让道具看起来像包装内含物。

### 2. 图片文字规则

- 能不使用文字就不使用文字。
- 必须使用文字时，默认统一采用精简英文，即使 Listing 使用德语、法语、意大利语或西班牙语。
- 单个标签或短语优先控制在 1—3 个词。
- 尺寸图只保留准确数值和单位。
- 流程图优先使用 01/02/03；确需动作词时使用极短英文。

### 3. 参考图如何选择

每张图先定义用途标签，再按用途筛选参考，例如：

- 主图：俯视或正视、完整轮廓、白底主体占比。
- 近照：产品前景与淡化使用背景的层次。
- 尺寸：测量箭头、数值层级、细节窗位置。
- 流程/细节：步骤关系或全貌加局部的结构。
- 特点：一个经过事实确认的差异化卖点。
- 场景：产品可见比例、环境透视和道具关系。
- 四宫格：多个场景之间的差异、统一性和阅读顺序。

竞品与当前变体在图案、颜色、结构和规格上逐项一致时，可以标为“同款产品参考”；否则只能标为“版式参考”。即使判定同款，也只能借鉴视觉外观，不能继承竞品品牌、尺寸、配件、文案或功能声明。

参考图数量按任务决定，不固定为两张。一张已经能完整说明构图和信息表达时只用一张；需要分别借鉴主体比例、信息层级、细节或场景时，可从不同畅销竞品中组合两张至四张，确有必要时再增加。每增加一张都必须在作图要求中说明独立用途，不能放重复或无关图片。

### 4. 缺少事实时如何降级

- 使用步骤不明确：从流程图改为结构或细节图。
- 没有可靠差异化优势：从特点图改为另一类场景图。
- 闭合方式不明确：不画绑带、魔术贴、纽扣等具体结构。
- 缺少会影响真实外观的证据：只在受影响任务内写明具体缺口，不建立通用“待补素材”区。
- 参考图不足：用文字明确构图目标，不编造不可访问的链接。

### 5. 多变体处理

多变体可以复用同一构图母版和参考图片，但图案、颜色或结构不同的变体必须分别生成成品。作图单会明确每个任务适用的变体、每款数量和不可改变项，避免把 A 款成品直接当成 B 款或 C 款。

### 6. Excel 交付结构

未指定模板时，作图交接采用两张轻量工作表：

- `产品内容`：每个变体一行，统一使用产品名称、产品图片、产品尺寸、产品材料、产品规格、补充信息。
- `作图要求`：图片顺序、产品名称、按实际最大需要设置的参考图片1…N、完整执行要求。

产品实拍只在“产品内容”嵌入一次；“作图要求”不重复设置产品图片列。竞品参考图直接嵌入对应单元格，不使用图片超链接；一张够用就只放一张，多张参考图必须分别承担明确作用。默认输出规格为 1:1、1254 × 1254 px。

## 一致性和质量检查

### Listing 检查

- 每个目标站点和在售变体都有完整 Listing。
- Item Name 为 68—75 字符，含 2—4 个已确认核心关键词；关键差异前置，主要场景可选。
- Item Highlights 必填且不超过 125 字符，补充关键词、规格、结构和场景。
- 每套 Listing 恰好 5 条高信息量卖点，每条正文超过 200 个可见字符；详情使用基础 HTML 并包含 2—3 句概述、3—5 项详细功能、参数和包装。
- 数字、单位、材质、数量、颜色、图案和结构可追溯到自有事实。
- 各站语言自然，不混入其他站点语言或竞品身份。
- Search Terms 仅含经卖家精灵反查与 Amazon 相关性复核、且前台未覆盖的真实相关表达；使用小写和空格分隔，不含重复词、停用词、标点、品牌、ASIN、错误规格、无依据功能或无关流量词，并不超过 249 UTF-8 字节。
- 连续长片段重复会提示复核，但未命中不等于证明原创。

### 作图要求检查

- 图序、图片数量、变体数量和最终成品数能够互相核对。
- 每张图都有明确目的、构图、文字、禁止项和验收条件。
- 嵌入的参考图片与当前图型任务匹配，而不是因为素材存在就随意选用。
- 尺寸线绑定真实测量部位，单位换算一致。
- 场景道具不会被表现为包装内容。
- 图片文字遵守“尽量无文字、必要时精简英文”的规则。
- 不生成未经确认的结构、纹理、厚度、配件和性能。

## 方法边界

这套方法能提高事实一致性、本地化质量和美工交接效率，但不等于：

- 已验证目标站点的搜索量、广告流量或转化率。
- 已核实所有站点和类目的最新发布限制。
- 已取得竞品图片的复制或商用授权。
- 已确认未写入开发文档的材料性能、认证和使用方式。
- 已完成 Amazon 后台发布、广告投放或实际商品图制作。

需要发布级限制、当地词频或广告数据时，应在对应任务中接入最新官方规则或可靠数据源，再更新 Listing 和检查结果。

## 怎么使用

### 推荐：使用运营总入口

上传开发 Excel 和可用的自有产品素材，然后说明目标站点和需要的成果。例如：

```text
使用 chenyu-yunying，读取这份开发表里的产品信息和竞品链接，
为德国、法国站生成自有产品 Listing，并生成交给美工的逐图作图要求，不加品牌。
```

总入口会：

1. 只读解析开发文档，区分自有事实、变体、竞品链接和素材。
2. 有竞品链接时调用 `chenyu-jingpin`，只抓取一次；任务要求时以各站点相关 Best Sellers 前 50 为候选池，不主动新增评论痛点分析。
3. 将同一个事实记录和研究包交给 Listing 与作图要求；Listing 按“Item Name 核心词、Item Highlights 规格/结构/场景、五点与详情自然展开、Search Terms 增量词”分配关键词。
4. 核对名称、材质、尺寸、数量、变体和卖点的一致性。

### 直接使用专业 Skill

需求边界明确时可以直接指定：

```text
使用 chenyu-jingpin，抓取表格中的 Amazon 竞品 Listing、普通/A+描述和完整图片。
```

```text
使用 chenyu-listing，基于这份自有产品事实和竞品研究包生成德国站 Listing，不加品牌。
```

```text
使用 chenyu-zuotuyaoqiu，根据产品素材和已完成的 Listing 生成逐张作图要求。
```

### 建议提供的信息

- 产品开发 Excel 或已整理的自有产品事实。
- 明确的目标站点：DE、FR、IT、ES、UK 中的一个或多个。
- 产品名称、材质、尺寸及测量部位、颜色/图案、结构和包装内容。
- 每个变体是否在目标站点销售。
- 产品实拍、供应商图、版式参考及它们分别对应哪个产品或变体。
- 需要 Listing、作图要求、竞品研究，还是它们的组合。

必要事实缺失时，Skill 会列出具体待确认项；不会用竞品规格填补自有产品信息。

## 主要任务文件

任务文件应放在插件目录以外的独立输出目录，不进入 Git 或分发包。

| 文件/目录 | 作用 |
| --- | --- |
| `brief/manifest.json` | 开发表的工作表、单元格、链接、图片锚点、附件和警告 |
| `competitors/competitor-research.json` | 去重后的竞品 Listing、榜单/图片证据、抓取状态和来源 |
| `listing-package.json` | 自有事实、关键词映射、站点/变体 Listing 和引用关系 |
| `package-review.json` | Listing 完整性、格式、事实引用和站点/变体覆盖检查 |
| `content-review.json` | 关键词覆盖、长度、原词/别名位置和竞品长片段复核 |
| 作图要求文件 | 每张图的变体、目的、构图、文案、嵌入式参考图片和验收条件 |

## 内置脚本

脚本使用 Python 3.10+ 标准库。以下路径均相对相应 Skill 目录，真实任务应使用插件外的输出目录。

```bash
# 解析开发 Excel
python skills/chenyu-yunying/scripts/extract_development_brief.py "input.xlsx" --out "task/brief"

# 抓取竞品 Listing 和图片
python skills/chenyu-jingpin/scripts/fetch_competitor_listings.py "task/brief/manifest.json" --out "task/competitors"

# 校验自有 Listing 包
python skills/chenyu-listing/scripts/validate_listing_package.py "task/listing-package.json" --out "task/package-review.json"

# 分析关键词覆盖、长度和重复片段
python skills/chenyu-listing/scripts/analyze_listing.py "task/listing-package.json" --out "task/content-review.json"
```

爬虫只访问任务提供的受支持 Amazon 商品页；遇到验证码、访问限制或字段缺失时记录 `partial/failed`，不会绕过限制或用搜索摘要补写内容。

## 目录结构

```text
chenyu-yunying/
├── plugin.json                       # 插件清单、版本和界面信息
├── README.md                         # 本说明
├── references/
│   └── kaifawendang.md               # 所有运营 Skill 共用的开发文档读取规范
└── skills/
    ├── chenyu-yunying/               # 运营总入口与 Excel 提取器
    ├── chenyu-jingpin/               # 竞品研究、爬虫和研究规范
    ├── chenyu-listing/               # Listing 写作、欧洲站本地化和校验脚本
    └── chenyu-zuotuyaoqiu/           # 逐图作图要求
```

每个 Skill 至少包含 `SKILL.md`。按实际需要增加：

- `agents/openai.yaml`：界面名称、简短说明、默认调用示例。
- `references/`：只在该 Skill 工作时需要读取的详细业务规范或数据契约。
- `scripts/`：需要重复、确定性执行的解析、抓取或校验程序。
- `assets/`：Skill 自带并会在输出中使用的模板、图标等静态资源。

不要建立空目录，也不要把真实业务样本放进 Skill。

## 如何新增一个 Skill

### 1. 先判断是否应该建 Skill

| 需求类型 | 应放在哪里 |
| --- | --- |
| 一个边界清楚、用户可单独请求、会产生独立成果的能力 | 新建 Skill |
| 只是某个 Skill 反复使用的确定性处理步骤 | 放进该 Skill 的 `scripts/` |
| 只是详细规则、字段说明或语言规范 | 放进该 Skill 的 `references/` |
| 需要读取 Amazon、ERP、素材库等外部实时系统 | 实现 MCP，并单独设计读取/写入权限 |
| 把多个 Skills 和人工确认点串成固定业务流程 | Workflow；实现前不要建空目录 |
| 代表一个岗位选择 Workflow、Skill 和工具 | Agent；等岗位流程与权限稳定后再实现 |

### 2. 创建目录和入口文件

在 `skills/` 下使用小写连字符名称，例如：

```text
skills/chenyu-guanggao/
├── SKILL.md
└── agents/openai.yaml
```

`SKILL.md` 最小结构：

```markdown
---
name: chenyu-guanggao
description: 清楚说明该能力处理什么请求、何时使用，以及不负责什么。
---

# Skill 名称

说明输入、处理规则、完成条件、交付格式和权限边界。
```

只有真正实现后才把新 Skill 加入插件；规划中的名称不能伪装成可用能力。

### 3. 接入运营总入口

新增专业 Skill 后同时更新：

1. `skills/chenyu-yunying/SKILL.md` 的任务分配表和共享工作流。
2. 本 README 的能力表、使用示例和目录结构。
3. `plugin.json` 的版本、说明和 `capabilities`（能力确实变化时）。
4. `AGENTS.md` 的仓库路由说明（如果仓库级路由发生变化）。

共享开发文档规则放在插件根目录 `references/`；仅属于新 Skill 的规则放在它自己的 `references/`，避免重复维护。

### 4. 验证

在仓库根目录运行：

```powershell
$env:PYTHONUTF8='1'
python "$env:USERPROFILE\.codex\skills\.system\skill-creator\scripts\quick_validate.py" "plugins\chenyu-yunying\skills\新-skill"
python -m unittest discover -s tests -v
```

至少检查：名称和目录一致、YAML frontmatter 有效、引用路径存在、脚本实际可运行、没有占位内容、没有真实产品数据、没有把尚未实现的 MCP/Agent/Workflow 写成可用能力。

## 更新现有 Skill

1. 先确认修改属于哪个专业 Skill，不把所有逻辑都堆进总入口。
2. 业务规则写入 `SKILL.md` 或对应 `references/`；可重复机械检查写入脚本。
3. 为新增的确定性行为补充测试。
4. 更新插件补丁或次版本号，并同步本 README。
5. 验证全部 Skills 和测试后再提交、推送、打包。

## 打包与安装

分发 ZIP 的根层必须直接包含：

```text
plugin.json
README.md
references/
skills/
```

不要在 ZIP 根层再套一层 `chenyu-yunying/`，也不要包含 `__pycache__`、`.pyc`、测试缓存、真实开发表、竞品图片或任务输出。

从仓库根目录用已提交版本打包：

```powershell
New-Item -ItemType Directory -Force outputs | Out-Null
git archive --format=zip --output="dist/chenyu-yunying-1.0.0.zip" HEAD:plugins/chenyu-yunying
```

员工在支持插件上传的管理页面选择该 ZIP，安装或更新后新建对话使用。更新包时只需要重新分发 `chenyu-yunying`，不会影响美工、开发或采购插件。

## 当前边界

- 支持 DE、FR、IT、ES、UK 的 Listing；只有确认自有品牌时才加品牌，否则保持无品牌。
- 只生成文案和作图要求，不自动制作商品图；实际作图交给美工插件。
- `chenyu-guanggao` 仍是规划能力，当前未实现。
- 没有后台任务、跨会话数据库或自动发布服务。
- 平台和类目限制需要发布级确认时，应核实当前官方规则；未知限制不会标记为已通过。

仓库总体结构和其他岗位插件见项目根目录的 [README](../../README.md)。
